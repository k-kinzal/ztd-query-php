<?php

declare(strict_types=1);

namespace SqlSemantics\Model\Validation;

/**
 * Human-readable descriptions of each classified input violation, kept apart from the closed violation domain.
 * @visibility SqlSemantics
 */
final class ViolationMessages
{
    /** @var array<string, string> */
    private const MESSAGES = [
        'ownership-role' => 'An ownership selector requires a concrete role or session role reference, excluding public and the reserved name none.',
        'routine-language' => 'A stored routine language requires a nonempty name.',
        'storage-name' => 'Storage DDL requires nonempty object and engine names.',
        'engine-name' => 'A storage engine is named by at least one character.',
        'storage-option' => 'Storage DDL cannot repeat the engine option or a consecutive legacy NO_WAIT request.',
        'storage-value' => 'Storage DDL sizes require an unsigned number or a K, M, or G multiple below 2^31 units, encryption requires Y or N, engine attributes require JSON, and NODEGROUP, COMMENT, and FILE_BLOCK_SIZE are given at most once.',
        'server-definition' => 'A MySQL server or loadable function definition requires a nonempty name and a port that fits a signed 64-bit integer.',
        'database-name' => 'A database operation requires a nonempty database name.',
        'database-read-only' => 'Database READ ONLY requires DEFAULT, zero, or one, and repeated requests must agree.',
        'database-encryption' => 'Database encryption requires Y or N.',
        'database-character-name' => 'A database character default requires a nonempty name.',
        'server-option' => 'Initial foreign server options require unique names.',
        'mapping-role' => 'A user mapping requires a nonempty role name other than the reserved name none.',
        'mapping-option' => 'Initial user mapping options require unique names.',
        'wrapper-function' => 'A foreign-data wrapper cannot specify a support function more than once.',
        'wrapper-option' => 'Initial foreign-data wrapper options require unique names.',
        'stored-table-clause' => 'Partition selection, INDEXED BY, NOT INDEXED and TABLESAMPLE apply to stored tables, not to common table expressions.',
        'recursive-query-clause' => 'SEARCH and CYCLE follow a WITH RECURSIVE query, list columns the query exposes, and add columns whose names are new and distinct.',
        'window-modifier' => 'MySQL window functions respect NULLs and count from the first row; IGNORE NULLS and FROM LAST are rejected.',
        'sample-arguments' => 'The built-in SYSTEM and BERNOULLI sampling methods take exactly one percentage argument.',
        'relation-name' => 'A relation name requires at most three identifier components without subscripts or wildcards.',
        'spatial-reference-id' => 'Spatial reference system DDL requires a nonzero unsigned 32-bit SRID and unsigned 32-bit organization identifiers.',
        'spatial-attribute' => 'A spatial definition requires exactly one NAME and DEFINITION and no duplicate attributes.',
        'type-parameter-count' => 'The built-in type does not accept this number of modifier operands.',
        'type-modifier' => 'PostgreSQL type modifiers require simple numeric or text constants or unqualified identifiers.',
        'routine-name' => 'A routine name requires identifier components without subscripts or wildcards.',
        'aggregate-argument-mode' => 'An aggregate signature accepts only input and variadic arguments.',
        'aggregate-variadic-signature' => 'A variadic direct argument requires one variadic aggregated argument of the same declared type.',
        'discarded-value' => 'DO requires scalar expressions and cannot expand table columns without a table input.',
        'histogram-target' => 'A histogram request requires exactly one target table.',
        'histogram-import' => 'Imported histogram data describes exactly one column.',
        'histogram-buckets' => 'A histogram bucket limit must be an integer from 1 to 1024.',
        'temporal-operand' => 'Temporal arithmetic requires one scalar temporal input and one scalar interval quantity.',
        'table-columns' => 'A table declaration requires at least one column in this SQL dialect.',
        'xa-identifier' => 'An XA identifier requires bounded byte-string components and a nonnegative integer format identifier.',
        'delete-target' => 'A MySQL DELETE destination requires a named table reference.',
        'extraction-field' => 'EXTRACT requires a field defined by the database language.',
        'query-operand' => 'A query operand requires a SELECT, VALUES, or TABLE operation.',
        'function-arity' => 'The function or conditional operation requires its declared number of arguments.',
        'reindex-option' => 'REINDEX requires a known option and a value in its declared domain.',
        'concurrent-system-reindex' => 'System-table indexes cannot be rebuilt concurrently.',
        'ordered-set-window' => 'An ordered-set aggregate cannot be used as a window function.',
        'default-context' => 'DEFAULT requires a column write or a setting assignment.',
        'foreign-key-column-count' => 'A foreign key requires matching local and referenced column counts.',
        'invalid-concurrent-index-drop' => 'Concurrent index deletion requires one index and cannot cascade.',
        'json-option' => 'The JSON option is outside the domain accepted by this operation.',
        'xml-option' => 'The XML column option is unknown or specified more than once.',
        'xml-value-name' => 'An XMLATTRIBUTES or XMLFOREST value without AS must be a column reference, and an element cannot repeat an attribute name.',
        'xml-serialization-target' => 'XMLSERIALIZE converts only to a character string type: text, character varying, character, name, or a domain over one.',
        'cursor-options' => 'A cursor cannot request both SCROLL and NO SCROLL.',
        'scalar-query-width' => 'A scalar subquery requires exactly one result column.',
        'comparison-width' => 'Compared row operands must have equal widths.',
        'joined-mutation-pagination' => 'A MySQL joined mutation cannot specify ORDER BY or LIMIT.',
        'qualified-trigger-target' => 'A SQLite trigger mutation requires an unqualified target name.',
        'trigger-index-hint' => 'A SQLite trigger mutation cannot specify an index hint.',
        'added-column-key' => 'SQLite ADD COLUMN cannot declare a PRIMARY KEY or UNIQUE constraint.',
        'multiple-primary-keys' => 'A table can declare only one primary key.',
        'nullable-primary-key' => 'MySQL 5.7.3 and later reject a PRIMARY KEY column declared NULL or DEFAULT NULL; every part of a primary key is NOT NULL.',
        'null-default' => 'MySQL rejects DEFAULT NULL on a NOT NULL column, including a column its own PRIMARY KEY attribute makes NOT NULL, unless the column is AUTO_INCREMENT (error 1067).',
        'conflicting-nullability' => 'PostgreSQL rejects a column declared both NULL and NOT NULL, including NULL on a serial or identity column, which is NOT NULL by definition.',
        'query-block-option' => 'HIGH_PRIORITY, SQL_CALC_FOUND_ROWS and SQL_BUFFER_RESULT, and before MySQL 8.0 SQL_CACHE and SQL_NO_CACHE, apply only to the first query block of the outermost query, and SQL_CACHE excludes SQL_NO_CACHE.',
        'autoincrement-key' => 'SQLite AUTOINCREMENT is only allowed on an INTEGER PRIMARY KEY that aliases the rowid.',
        'invalid-output-position' => 'ORDER BY position is outside the result.',
        'invalid-explain-option' => 'The EXPLAIN option is not defined by this database language.',
        'invalid-explain-setting' => 'The EXPLAIN option requires a value in its declared domain.',
        'invalid-explain-combination' => 'The EXPLAIN options have incompatible execution requirements.',
        'assignment-column-count' => 'Assignment destinations and values must have the same width.',
        'assignment-destination' => 'An assignment destination must be a column, or a field or array element of a column.',
        'tuple-source' => 'A tuple assignment requires a row constructor or a row-producing query.',
        'insert-column-count' => 'INSERT destinations and input values must have the same width.',
        'values-column-count' => 'VALUES rows must have the same width.',
        'set-column-count' => 'Set-operation operands must have the same result width.',
        'set-operation-lock' => 'PostgreSQL does not allow FOR UPDATE, FOR NO KEY UPDATE, FOR SHARE, or FOR KEY SHARE with UNION, INTERSECT, or EXCEPT, neither on the set operation nor on its operands.',
        'values-lock' => 'PostgreSQL does not allow FOR UPDATE, FOR NO KEY UPDATE, FOR SHARE, or FOR KEY SHARE with VALUES.',
        'derived-lock-target' => 'A MySQL locking clause names only stored tables and common table expressions after OF, never a derived table or JSON_TABLE (error 3569).',
        'repeated-lock-target' => 'A MySQL query block locks each table in at most one locking clause: a table is not named twice after OF, and a clause without OF, which locks every table, is the only clause.',
        'temporary-commit-action' => 'PostgreSQL accepts ON COMMIT PRESERVE ROWS, DELETE ROWS or DROP only on a temporary table.',
        'table-creation-lock' => 'MySQL cannot fill a new table from a query that locks a stored table FOR UPDATE, in any of its query blocks.',
        'invalid-alter-algorithm' => 'The requested ALTER algorithm is not defined by this database language.',
        'invalid-alter-lock' => 'The requested ALTER lock mode is not defined by this database language.',
        'concurrent-empty-refresh' => 'A concurrent materialized view refresh cannot leave the view without data.',
        'cte-column-count' => 'CTE aliases must match the declared query result positions.',
        'quantified-operator' => 'ANY, SOME and ALL over an array require an operator that yields boolean.',
        'overlaps-width' => 'Each side of OVERLAPS requires exactly a start and an end.',
        'unique-predicate' => 'PostgreSQL does not implement the UNIQUE predicate.',
        'derived-column-count' => 'Derived table column aliases must not exceed, and in MySQL must equal, the query result width.',
        'insert-row-alias' => 'An INSERT row alias cannot repeat the table name, and its column aliases name each inserted column exactly once.',
        'derived-column-name' => 'A MySQL derived table or common table expression cannot name two columns the same.',
        'duplicate-cte' => 'A WITH clause cannot define the same relation name twice.',
        'duplicate-relation' => 'A FROM clause cannot name two items the same: an alias cannot repeat another alias or table name, and a table cannot appear twice without an alias.',
        'temporary-table-schema' => 'A temporary table is created in the temporary schema: PostgreSQL accepts no schema or pg_temp, SQLite no schema or temp.',
        'role-name' => 'A role definition, deletion, or rename requires a concrete role name; session role references, public, and none are reserved.',
        'role-reference' => 'A role reference requires a role name or a session role reference; public and none are not roles here.',
        'role-option' => 'A role option must be a known attribute for this operation and cannot be repeated or contradicted.',
        'role-numeric-option' => 'A connection limit requires a 32-bit integer of at least -1 and a system identifier a nonnegative 32-bit integer.',
        'role-password-encryption' => 'UNENCRYPTED PASSWORD is no longer supported.',
        'role-setting' => 'A role setting requires a configuration parameter assignment or reset, not a transaction characteristic.',
        'privilege-name' => 'A privilege must be one of the privilege types defined by the database.',
        'privilege-target' => 'The privilege type is not defined for this object class.',
        'column-privilege' => 'Column lists apply only to SELECT, INSERT, UPDATE, REFERENCES, and ALL PRIVILEGES on tables.',
        'large-object-id' => 'A large object identifier requires an unsigned 32-bit integer.',
        'role-grant-option' => 'A role membership option must be ADMIN, INHERIT, or SET.',
        'default-privilege-scope' => 'Default privileges accept one FOR ROLE clause, one IN SCHEMA clause, and no schema selection for SCHEMAS.',
        'authentication-plugin' => 'An authentication plugin requires a nonempty name.',
        'authentication-factor' => 'A factor number must be 2 or 3, added factors ascend, and a change cannot address the same factor twice.',
        'account-limit' => 'An account limit or resource limit requires an integer count in the range MySQL accepts for its kind.',
        'certificate-requirement' => 'REQUIRE cannot repeat SUBJECT, ISSUER, or CIPHER.',
        'role-grant' => 'A role list names roles and a privilege list names privileges; neither accepts the other kind of element.',
        'privilege-level' => 'The privilege is not defined at this level: dynamic privileges are global, routine levels take EXECUTE, ALTER ROUTINE, and GRANT OPTION, and a routine class cannot qualify a wildcard.',
        'revoked-credential' => 'A revocation cannot supply account credentials.',
        'catalog-object-name' => 'The object name does not have the number of components its object class requires.',
        'operator-signature' => 'An operator signature requires two operand positions; NONE marks the missing operand of a unary operator.',
        'security-label-target' => 'Security labels are not supported for this object class.',
        'constraint-attribute' => 'NOT VALID applies to check and foreign key constraints, NO INHERIT to check constraints, and INITIALLY DEFERRED requires DEFERRABLE.',
        'reset-parameter-value' => 'RESET names parameters without values.',
        'channel-name' => 'A replication channel name cannot contain a line feed.',
        'partition-bound' => 'A partition bound requires a modulus and a remainder below it, or range ends of equal width.',
        'sequence-attribute' => 'A sequence attribute requires an integer literal.',
        'identity-option' => 'AS, RESTART, and OWNED BY are not options of an identity column change; RESTART stands on its own.',
        'column-compression' => 'Column compression is pglz, lz4, or DEFAULT.',
        'column-storage' => 'Column storage is PLAIN, EXTERNAL, EXTENDED, MAIN, or DEFAULT.',
        'column-position' => 'A column position is an integer between 1 and 32767.',
        'statistics-target' => 'A statistics target is DEFAULT or an integer between -1 and 10000.',
        'foreign-table-constraint' => 'Foreign tables accept only NOT NULL and CHECK constraints.',
        'instance-action' => 'ALTER INSTANCE rotates the INNODB or BINLOG master key, reloads the mysql_main or mysql_admin TLS context, and enables or disables only INNODB REDO_LOG.',
        'replication-option' => 'A replication option requires a value in its domain: single-line text, a password of at most 32 characters, a delay or heartbeat period in range, 0 or 1 where only those are accepted, and a UUID for anonymous transaction GTIDs.',
        'replica-until' => 'START REPLICA UNTIL requires one complete source or relay log position, a GTID set, or SQL_AFTER_MTS_GAPS alone.',
        'replica-credentials' => 'Replication credentials cannot accompany a request that starts only the SQL thread; a group replication user is nonempty and its password has at most 32 characters.',
        'binary-log-index' => 'A binary log reset index is an integer from 1 to 2000000000.',
        'source-coordinates' => 'Log coordinates cannot be set together with SOURCE_AUTO_POSITION = 1, and source log coordinates cannot be set together with relay log coordinates.',
        'replication-filter' => 'A wildcard replication table filter pattern is single-line text that separates the database and table with a dot.',
        'cast-conversion' => 'MySQL does not support CAST ... AT LOCAL; a time zone cast converts AT TIME ZONE to DATETIME.',
        'sequence-option' => 'Identity sequence options cannot repeat or conflict, and AS names smallint, integer, or bigint.',
        'existing-index-constraint' => 'CREATE TABLE cannot build a constraint on an existing index with USING INDEX.',
        'partition-key' => 'A partitioning strategy is RANGE, LIST, or HASH, and LIST takes exactly one partition key.',
        'partitioned-table' => 'A partitioned table cannot inherit from another table or declare storage parameters.',
        'inherited-parent' => 'A table inherits from each parent table once.',
        'column-override' => 'A partition or typed table overrides each of its columns at most once.',
        'table-option' => 'A MySQL table option requires a value of its kind: an unsigned integer within the signed 64-bit range, 0 or 1 for a switch, 1 to 65535 sampled pages, or a size with a K, M, G, or T suffix.',
        'table-alteration' => 'A MySQL table alteration requires nonempty names, a known ALGORITHM and LOCK, and operands the server accepts.',
        'partition-definition' => 'MySQL partitioning requires positive integer counts, RANGE or LIST values that match the partitioning columns, and unique partition names.',
        'index-prefix' => 'An index key prefix length must be a positive integer.',
        'database-option' => 'A PostgreSQL database option must be known for the operation, appear once, and carry a value in its declared domain.',
        'stored-setting' => 'A stored database or system setting requires a configuration parameter assignment or reset, not a session-only command.',
        'schema-element' => 'CREATE SCHEMA IF NOT EXISTS cannot include schema elements, and each element must be created in the new schema.',
        'maintenance-option' => 'VACUUM, ANALYZE, and CLUSTER require known options with values in their domains, compatible combinations, and column lists only with ANALYZE.',
        'copy-option' => 'COPY requires known unrepeated options valid for its format and direction, a named file or program, and a query that returns rows.',
        'anonymous-block' => 'DO requires exactly one code block and at most one language.',
        'function-table-columns' => 'A function table allows no more column aliases than columns, unique defined columns, and one column definition list per function, written outside ROWS FROM only for a single function without WITH ORDINALITY.',
        'procedure-call' => 'CALL takes positional arguments before named ones, unique argument names, and no aggregate modifiers.',
        'function-argument-notation' => 'A function call takes positional arguments before named ones, names each parameter at most once, and marks only its last argument VARIADIC.',
        'select-into' => 'SELECT ... INTO appears once, on the first SELECT in PostgreSQL or after the last query block in MySQL, never in a subquery or WITH declaration, and names one variable per selected column.',
        'row-expansion' => 'Row expansion with .* ends a reference to a relation or a composite value and is not followed by further field or element access.',
        'json-predicate-operand' => 'IS JSON tests a text, JSON or bytea value.',
        'tablespace-option' => 'A tablespace requires a nonempty name, an absolute location without quotes, and known cost or concurrency parameters, each given once: a cost is a finite non-negative real, a concurrency an integer from 0 to 1000.',
        'domain-constraint' => 'A domain accepts only CHECK, NOT NULL, and NULL constraints without deferrability, one DEFAULT and one COLLATE clause, no conflicting NULL and NOT NULL, NO INHERIT only when altering, and NOT VALID only on an added CHECK constraint.',
        'set-of-declaration' => 'SETOF cannot declare an operator operand or an aggregate argument.',
        'enum-label' => 'An enum label is at most 63 bytes long, is listed once, is renamed to a different label, and cannot be dropped.',
        'definition-attribute' => 'A definition names only the attributes its object class recognizes where unknown names are rejected, repeats none where repetition is rejected, and ALTER changes only the attributes that can change.',
        'definition-argument' => 'A definition attribute requires an argument of its form: a type, a name, a Boolean, an integer, a type length, or one of its keywords.',
        'definition-requirement' => 'A definition supplies the attributes its object class requires and no combination of attributes the server rejects.',
        'operator-class-member' => 'Operator class and family members use strategy and support numbers from 1 to 32767, binary operators, one or two associated types, and STORAGE only once and only in CREATE OPERATOR CLASS; ALTER OPERATOR FAMILY ADD requires operator operand types.',
        'conversion-encoding' => 'An encoding conversion names two known server encodings other than SQL_ASCII.',
        'composite-attribute' => 'A composite type declares each attribute name once and no attribute as SETOF, and one ALTER TYPE command adds each attribute once.',
        'cast-definition' => 'A cast without a function joins two different types, and a binary-coercible cast joins no array types.',
        'stored-program-reference' => 'A local variable, condition name or column reference is only declared inside a stored program.',
        'signal-information' => 'SIGNAL and RESIGNAL require a valid SQLSTATE other than a completion condition and set each condition item at most once to a literal or variable other than NULL.',
        'resource-group-option' => 'A resource group requires a name of 1 to 64 characters, CPU ranges whose first CPU is not after the last, a thread priority the group type allows, and integer thread identifiers.',
        'load-option' => 'LOAD DATA and LOAD XML require single-byte enclosure and escape characters, COUNT with a positive file count, integer or K, M, G memory sizes, URL, COUNT and COMPRESSION only with ALGORITHM = BULK, and no XML, LOCAL, column list, SET, IGNORE, LINES STARTING BY or multi-byte field terminator with ALGORITHM = BULK.',
        'session-setting' => 'SET NAMES takes a character set name, optionally with COLLATE, or DEFAULT rather than an assigned value, and a system variable name has at most one prefix, which cannot be GLOBAL, LOCAL or SESSION.',
        'trigger-definition' => 'A trigger names each event once; INSTEAD OF fires for each row of INSERT, UPDATE or DELETE without WHEN or columns; TRUNCATE fires for each statement; transition tables are OLD or NEW TABLE of an AFTER trigger on one row change without columns, each named once and differently; a constraint trigger cannot be replaced; and an event trigger fires on ddl_command_start, ddl_command_end, sql_drop, table_rewrite or login, filtered at most by one TAG list of command tags that support the event.',
        'rewrite-rule' => 'A rule on SELECT replaces a view\'s _RETURN rule and does INSTEAD exactly one query without a WHERE condition; a conditional rule cannot NOTIFY; and RETURNING belongs to one action of an unconditional INSTEAD rule.',
        'row-security-policy' => 'A policy is PERMISSIVE or RESTRICTIVE; a SELECT or DELETE policy has no WITH CHECK expression and an INSERT policy no USING expression.',
        'publication-object' => 'A publication object list starts with TABLE or TABLES IN SCHEMA; a schema is one unqualified name without column list or WHERE; column lists cannot accompany a schema; a table listed twice has neither; a removed table takes neither; and ALTER PUBLICATION SET changes at least one option.',
        'extension-option' => 'CREATE EXTENSION gives SCHEMA, VERSION and CASCADE at most once and no longer supports FROM, a version is nonempty, and ALTER EXTENSION UPDATE names at most one target version.',
        'routine-definition' => 'A routine has exactly one body: a string definition with a LANGUAGE, a second AS string only for language C, or an inline SQL body with language SQL; a function without RETURNS has OUT or INOUT parameters.',
        'routine-attribute' => 'A routine gives each attribute at most once; a procedure accepts only LANGUAGE, TRANSFORM, SECURITY, SET and AS; PARALLEL is SAFE, RESTRICTED or UNSAFE; COST and ROWS are positive and ROWS requires a set-returning function.',
        'routine-parameter' => 'Routine parameters take no SETOF types or duplicate names, only input parameters take defaults and every later input has one, VARIADIC is the last input, and RETURNS TABLE accepts only input parameters.',
        'statistics-definition' => 'Extended statistics read one plain table, name only the kinds ndistinct, dependencies and mcv, cover two to eight distinct columns or expressions without subqueries, or one expression without kinds.',
        'sequence-definition' => 'A sequence gives each option once, takes neither SEQUENCE NAME nor LOGGED or UNLOGGED as options, is owned by a table column or NONE, and has a nonzero increment, a positive cache, and bounds within its type with START and RESTART between them.',
        'stored-program-declaration' => 'A stored program block declares variables and conditions before cursors and cursors before handlers, each variable, condition and cursor name and each handled condition once, error numbers other than 0, valid non-completion SQLSTATE values, cursor queries without INTO, and before MySQL 8.0 a COLLATE only after a CHARACTER SET.',
        'stored-program-label' => 'A stored program label is not reused by an enclosing block or loop, an end label repeats its begin label, LEAVE names an enclosing label and ITERATE an enclosing loop, not crossing a handler.',
        'stored-program-object' => 'OPEN, FETCH and CLOSE name a declared cursor, FETCH INTO and SELECT INTO without @ name declared variables, a handler names a declared condition, a trigger reads OLD and NEW only when its event supplies them and assigns only NEW columns before the event, and DEFAULT is never assigned to a local variable or trigger column.',
        'stored-program-statement' => 'A stored program does not create or drop routines or triggers, define events with bodies, ALTER VIEW, USE, LOCK, UNLOCK, LOAD DATA or HELP; RETURN appears only in, and at least once in, a stored function; and stored functions and triggers neither return result sets nor commit, flush, reset or prepare statements.',
        'stored-program-definition' => 'A SQL routine has a compound-statement body and a routine in another language a string body, parameters are named once, ALTER EVENT changes at least one property, and an event interval is not measured in microseconds.',
    ];

    /**
     * Returns the operand invariant described by a violation.
     */
    public static function of(InputViolation $violation): string
    {
        return self::MESSAGES[$violation->value];
    }
}
