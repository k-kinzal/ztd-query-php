[
  {
    "id": "POSTGRESQL-SPEC-IDENTIFIER-CONTEXT",
    "level": "spec",
    "dialect": "postgresql",
    "statement": "WHERE PostgreSQL identifier contexts are supported, the generator SHALL emit canonical identifiers that remain acceptable to the engine lexer.",
    "subject": { "kind": "family", "family": "postgresql.lex.identifier.context" },
    "evidence": [
      { "kind": "witness.generates" },
      { "kind": "outcome.kind_in", "allowedKinds": ["accepted", "state"] }
    ]
  },
  {
    "id": "POSTGRESQL-SPEC-ALTER-MATERIALIZED-VIEW",
    "level": "spec",
    "dialect": "postgresql",
    "statement": "WHERE ALTER MATERIALIZED VIEW is retained, the generator SHALL produce engine-acceptable witnesses from that object-specific family.",
    "subject": { "kind": "family", "family": "postgresql.constraint.alter_materialized_view" },
    "evidence": [
      { "kind": "witness.generates" },
      { "kind": "outcome.kind_in", "allowedKinds": ["accepted", "state"] }
    ]
  },
  {
    "id": "POSTGRESQL-SPEC-ALTER-INDEX-COMMANDS",
    "level": "spec",
    "dialect": "postgresql",
    "statement": "WHERE ALTER INDEX commands are retained, the generator SHALL produce engine-acceptable witnesses from the index-specific command subset.",
    "subject": { "kind": "family", "family": "postgresql.constraint.alter_index.commands" },
    "evidence": [
      { "kind": "witness.generates" },
      { "kind": "outcome.kind_in", "allowedKinds": ["accepted", "state"] }
    ]
  },
  {
    "id": "POSTGRESQL-SPEC-ALTER-VIEW-COMMANDS",
    "level": "spec",
    "dialect": "postgresql",
    "statement": "WHERE ALTER VIEW commands are retained, the generator SHALL produce engine-acceptable witnesses from the view-specific command subset.",
    "subject": { "kind": "family", "family": "postgresql.constraint.alter_view.commands" },
    "evidence": [
      { "kind": "witness.generates" },
      { "kind": "outcome.kind_in", "allowedKinds": ["accepted", "state"] }
    ]
  },
  {
    "id": "POSTGRESQL-SPEC-ALTER-DOMAIN-ADD",
    "level": "spec",
    "dialect": "postgresql",
    "statement": "WHERE ALTER DOMAIN ADD is retained, the generator SHALL produce engine-acceptable witnesses from that constraint family.",
    "subject": { "kind": "family", "family": "postgresql.constraint.alter_domain.add" },
    "evidence": [
      { "kind": "witness.generates" },
      { "kind": "outcome.kind_in", "allowedKinds": ["accepted", "state"] }
    ]
  },
  {
    "id": "POSTGRESQL-SPEC-ALTER-SEQUENCE-COMMANDS",
    "level": "spec",
    "dialect": "postgresql",
    "statement": "WHERE ALTER SEQUENCE table-command statements are retained, the generator SHALL produce witnesses inside the sequence-specific command subset.",
    "subject": { "kind": "family", "family": "postgresql.constraint.alter_sequence" },
    "evidence": [
      { "kind": "witness.generates" },
      { "kind": "outcome.kind_in", "allowedKinds": ["accepted", "state"] }
    ]
  },
  {
    "id": "POSTGRESQL-SPEC-ALTER-STATISTICS-VALUE",
    "level": "spec",
    "dialect": "postgresql",
    "statement": "WHERE ALTER STATISTICS is retained, the generator SHALL produce witnesses inside the canonical positive integer statistics subset.",
    "subject": { "kind": "family", "family": "postgresql.constraint.alter_statistics" },
    "evidence": [
      { "kind": "witness.generates" },
      { "kind": "outcome.kind_in", "allowedKinds": ["accepted", "state"] }
    ]
  },
  {
    "id": "POSTGRESQL-SPEC-ALTER-ROUTINE-OPTIONS",
    "level": "spec",
    "dialect": "postgresql",
    "statement": "WHERE ALTER FUNCTION, PROCEDURE, and ROUTINE statements are retained, the generator SHALL produce witnesses inside the duplicate-free safe option subset.",
    "subject": { "kind": "family", "family": "postgresql.constraint.alter_routine.options" },
    "evidence": [
      { "kind": "witness.generates" },
      { "kind": "outcome.kind_in", "allowedKinds": ["accepted", "state"] }
    ]
  },
  {
    "id": "POSTGRESQL-SPEC-ALTER-TYPE-OPTIONS",
    "level": "spec",
    "dialect": "postgresql",
    "statement": "WHERE ALTER TYPE option statements are retained, the generator SHALL produce witnesses inside the safe option-name subset.",
    "subject": { "kind": "family", "family": "postgresql.constraint.alter_type.options" },
    "evidence": [
      { "kind": "witness.generates" },
      { "kind": "outcome.kind_in", "allowedKinds": ["accepted", "state"] }
    ]
  },
  {
    "id": "POSTGRESQL-SPEC-ALTER-DATABASE-OPTIONS",
    "level": "spec",
    "dialect": "postgresql",
    "statement": "WHERE ALTER DATABASE option statements are retained, the generator SHALL produce witnesses inside the accepted option subset.",
    "subject": { "kind": "family", "family": "postgresql.constraint.alter_database.options" },
    "evidence": [
      { "kind": "witness.generates" },
      { "kind": "outcome.kind_in", "allowedKinds": ["accepted", "state"] }
    ]
  },
  {
    "id": "POSTGRESQL-SPEC-CTAS-EXPLICIT-COLUMNS",
    "level": "spec",
    "dialect": "postgresql",
    "statement": "WHERE explicit CREATE TABLE AS column lists are supported, the generator SHALL produce accepted witnesses for arities 1 through 8.",
    "subject": { "kind": "family", "family": "postgresql.constraint.create_table_as.explicit_columns" },
    "cases": [
      { "parameters": { "arity": 1 } },
      { "parameters": { "arity": 2 } },
      { "parameters": { "arity": 3 } },
      { "parameters": { "arity": 4 } },
      { "parameters": { "arity": 5 } },
      { "parameters": { "arity": 6 } },
      { "parameters": { "arity": 7 } },
      { "parameters": { "arity": 8 } }
    ],
    "evidence": [
      { "kind": "witness.generates" },
      { "kind": "witness.property_equals_parameter", "property": "projection_arity", "parameter": "arity" },
      { "kind": "witness.property_equals_parameter", "property": "column_list_arity", "parameter": "arity" },
      { "kind": "outcome.kind_in", "allowedKinds": ["accepted", "state"] }
    ]
  },
  {
    "id": "POSTGRESQL-SPEC-ALTER-ROLE-VALID-UNTIL",
    "level": "spec",
    "dialect": "postgresql",
    "statement": "WHERE ALTER ROLE VALID UNTIL is retained, the generator SHALL produce engine-acceptable witnesses from that timestamp-domain family.",
    "subject": { "kind": "family", "family": "postgresql.constraint.alter_role.valid_until" },
    "evidence": [
      { "kind": "witness.generates" },
      { "kind": "outcome.kind_in", "allowedKinds": ["accepted", "state"] }
    ]
  },
  {
    "id": "POSTGRESQL-SPEC-CREATE-ROLE-NAME-AND-OPTIONS",
    "level": "spec",
    "dialect": "postgresql",
    "statement": "WHERE CREATE ROLE is retained, the generator SHALL produce witnesses inside the canonical role-name and singleton-option subset.",
    "subject": { "kind": "family", "family": "postgresql.constraint.create_role.name_and_options" },
    "evidence": [
      { "kind": "witness.generates" },
      { "kind": "outcome.kind_in", "allowedKinds": ["accepted", "state"] }
    ]
  },
  {
    "id": "POSTGRESQL-SPEC-DROP-ROLE-NAME-LIST",
    "level": "spec",
    "dialect": "postgresql",
    "statement": "WHERE DROP ROLE, DROP USER, and DROP GROUP are retained, the generator SHALL produce witnesses inside the canonical safe role-name subset.",
    "subject": { "kind": "family", "family": "postgresql.constraint.drop_role.name_list" },
    "evidence": [
      { "kind": "witness.generates" },
      { "kind": "outcome.kind_in", "allowedKinds": ["accepted", "state"] }
    ]
  },
  {
    "id": "POSTGRESQL-SPEC-GRANT-ROLE-NAME-LIST",
    "level": "spec",
    "dialect": "postgresql",
    "statement": "WHERE GRANT role membership is retained, the generator SHALL produce witnesses inside the canonical safe role-name subset.",
    "subject": { "kind": "family", "family": "postgresql.constraint.grant_role.name_list" },
    "evidence": [
      { "kind": "witness.generates" },
      { "kind": "outcome.kind_in", "allowedKinds": ["accepted", "state"] }
    ]
  },
  {
    "id": "POSTGRESQL-SPEC-REVOKE-ROLE-NAME-LIST",
    "level": "spec",
    "dialect": "postgresql",
    "statement": "WHERE REVOKE role membership is retained, the generator SHALL produce witnesses inside the canonical safe role-name subset.",
    "subject": { "kind": "family", "family": "postgresql.constraint.revoke_role.name_list" },
    "evidence": [
      { "kind": "witness.generates" },
      { "kind": "outcome.kind_in", "allowedKinds": ["accepted", "state"] }
    ]
  },
  {
    "id": "POSTGRESQL-SPEC-CREATE-USER-NAME-AND-OPTIONS",
    "level": "spec",
    "dialect": "postgresql",
    "statement": "WHERE CREATE USER is retained, the generator SHALL produce witnesses inside the canonical role-name and singleton-option subset.",
    "subject": { "kind": "family", "family": "postgresql.constraint.create_user.name_and_options" },
    "evidence": [
      { "kind": "witness.generates" },
      { "kind": "outcome.kind_in", "allowedKinds": ["accepted", "state"] }
    ]
  },
  {
    "id": "POSTGRESQL-SPEC-CREATE-GROUP-NAME-AND-OPTIONS",
    "level": "spec",
    "dialect": "postgresql",
    "statement": "WHERE CREATE GROUP is retained, the generator SHALL produce witnesses inside the canonical role-name and singleton-option subset.",
    "subject": { "kind": "family", "family": "postgresql.constraint.create_group.name_and_options" },
    "evidence": [
      { "kind": "witness.generates" },
      { "kind": "outcome.kind_in", "allowedKinds": ["accepted", "state"] }
    ]
  },
  {
    "id": "POSTGRESQL-SPEC-ALTER-EXTENSION-CONTENT-TARGET",
    "level": "spec",
    "dialect": "postgresql",
    "statement": "WHERE ALTER EXTENSION content members are retained, the generator SHALL produce witnesses inside the extension-valid member object subset.",
    "subject": { "kind": "family", "family": "postgresql.constraint.alter_extension.content_target" },
    "evidence": [
      { "kind": "witness.generates" },
      { "kind": "outcome.kind_in", "allowedKinds": ["accepted", "state"] }
    ]
  },
  {
    "id": "POSTGRESQL-SPEC-GRANT-LARGE-OBJECT-TARGET",
    "level": "spec",
    "dialect": "postgresql",
    "statement": "WHERE GRANT and REVOKE large-object targets are retained, the generator SHALL produce witnesses inside the canonical integer OID subset.",
    "subject": { "kind": "family", "family": "postgresql.constraint.grant.large_object_target" },
    "evidence": [
      { "kind": "witness.generates" },
      { "kind": "outcome.kind_in", "allowedKinds": ["accepted", "state"] }
    ]
  },
  {
    "id": "POSTGRESQL-SPEC-CREATE-TABLE-PARTITION-STRATEGY",
    "level": "spec",
    "dialect": "postgresql",
    "statement": "WHERE CREATE TABLE partitioning is retained, the generator SHALL produce witnesses inside the canonical partition-strategy subset.",
    "subject": { "kind": "family", "family": "postgresql.constraint.create_table.partition_strategy" },
    "evidence": [
      { "kind": "witness.generates" },
      { "kind": "outcome.kind_in", "allowedKinds": ["accepted", "state"] }
    ]
  },
  {
    "id": "POSTGRESQL-SPEC-CREATE-TABLE-PARTITION-OF",
    "level": "spec",
    "dialect": "postgresql",
    "statement": "WHERE CREATE TABLE PARTITION OF is retained, the generator SHALL produce engine-acceptable witnesses from the non-temporary subset.",
    "subject": { "kind": "family", "family": "postgresql.constraint.create_table.partition_of" },
    "evidence": [
      { "kind": "witness.generates" },
      { "kind": "outcome.kind_in", "allowedKinds": ["accepted", "state"] }
    ]
  },
  {
    "id": "POSTGRESQL-SPEC-CREATE-TABLE-TEMP-NAME-BINDING",
    "level": "spec",
    "dialect": "postgresql",
    "statement": "WHERE temporary CREATE TABLE statements are retained, the generator SHALL bind them only to unqualified canonical relation names.",
    "subject": { "kind": "family", "family": "postgresql.constraint.create_table.temp_name_binding" },
    "cases": [
      { "parameters": { "schema_qualified": false } }
    ],
    "evidence": [
      { "kind": "witness.generates" },
      { "kind": "witness.property_equals_parameter", "property": "schema_qualified", "parameter": "schema_qualified" },
      { "kind": "outcome.kind_in", "allowedKinds": ["accepted", "state"] }
    ]
  },
  {
    "id": "POSTGRESQL-SPEC-CTAS-TEMP-NAME-BINDING",
    "level": "spec",
    "dialect": "postgresql",
    "statement": "WHERE temporary CREATE TABLE AS statements are retained, the generator SHALL bind them only to unqualified canonical relation names.",
    "subject": { "kind": "family", "family": "postgresql.constraint.create_table_as.temp_name_binding" },
    "cases": [
      { "parameters": { "schema_qualified": false } }
    ],
    "evidence": [
      { "kind": "witness.generates" },
      { "kind": "witness.property_equals_parameter", "property": "schema_qualified", "parameter": "schema_qualified" },
      { "kind": "outcome.kind_in", "allowedKinds": ["accepted", "state"] }
    ]
  },
  {
    "id": "POSTGRESQL-SPEC-EXECUTE-CTAS-TEMP-NAME-BINDING",
    "level": "spec",
    "dialect": "postgresql",
    "statement": "WHERE temporary CREATE TABLE AS EXECUTE statements are retained, the generator SHALL bind them only to unqualified canonical relation names.",
    "subject": { "kind": "family", "family": "postgresql.constraint.execute_create_table_as.temp_name_binding" },
    "cases": [
      { "parameters": { "schema_qualified": false } }
    ],
    "evidence": [
      { "kind": "witness.generates" },
      { "kind": "witness.property_equals_parameter", "property": "schema_qualified", "parameter": "schema_qualified" },
      { "kind": "outcome.kind_in", "allowedKinds": ["accepted", "state"] }
    ]
  },
  {
    "id": "POSTGRESQL-SPEC-CREATE-SEQUENCE-TEMP-NAME-BINDING",
    "level": "spec",
    "dialect": "postgresql",
    "statement": "WHERE temporary CREATE SEQUENCE statements are retained, the generator SHALL bind them only to unqualified canonical relation names.",
    "subject": { "kind": "family", "family": "postgresql.constraint.create_sequence.temp_name_binding" },
    "cases": [
      { "parameters": { "schema_qualified": false } }
    ],
    "evidence": [
      { "kind": "witness.generates" },
      { "kind": "witness.property_equals_parameter", "property": "schema_qualified", "parameter": "schema_qualified" },
      { "kind": "outcome.kind_in", "allowedKinds": ["accepted", "state"] }
    ]
  },
  {
    "id": "POSTGRESQL-SPEC-CREATE-VIEW-EXPLICIT-COLUMNS",
    "level": "spec",
    "dialect": "postgresql",
    "statement": "WHERE CREATE VIEW explicit columns are retained, the generator SHALL produce accepted witnesses for arities 1 through 8.",
    "subject": { "kind": "family", "family": "postgresql.constraint.create_view.explicit_columns" },
    "cases": [
      { "parameters": { "arity": 1 } },
      { "parameters": { "arity": 2 } },
      { "parameters": { "arity": 3 } },
      { "parameters": { "arity": 4 } },
      { "parameters": { "arity": 5 } },
      { "parameters": { "arity": 6 } },
      { "parameters": { "arity": 7 } },
      { "parameters": { "arity": 8 } }
    ],
    "evidence": [
      { "kind": "witness.generates" },
      { "kind": "witness.property_equals_parameter", "property": "projection_arity", "parameter": "arity" },
      { "kind": "witness.property_equals_parameter", "property": "column_list_arity", "parameter": "arity" },
      { "kind": "outcome.kind_in", "allowedKinds": ["accepted", "state"] }
    ]
  },
  {
    "id": "POSTGRESQL-SPEC-CREATE-VIEW-TEMP-NAME-BINDING",
    "level": "spec",
    "dialect": "postgresql",
    "statement": "WHERE temporary CREATE VIEW statements are retained, the generator SHALL bind them only to unqualified canonical relation names.",
    "subject": { "kind": "family", "family": "postgresql.constraint.create_view.temp_name_binding" },
    "cases": [
      { "parameters": { "schema_qualified": false } }
    ],
    "evidence": [
      { "kind": "witness.generates" },
      { "kind": "witness.property_equals_parameter", "property": "schema_qualified", "parameter": "schema_qualified" },
      { "kind": "outcome.kind_in", "allowedKinds": ["accepted", "state"] }
    ]
  },
  {
    "id": "POSTGRESQL-SPEC-INSERT-EXPLICIT-COLUMNS",
    "level": "spec",
    "dialect": "postgresql",
    "statement": "WHERE INSERT explicit columns are retained, the generator SHALL produce accepted witnesses for arities 1 through 8.",
    "subject": { "kind": "family", "family": "postgresql.constraint.insert.explicit_columns" },
    "cases": [
      { "parameters": { "arity": 1 } },
      { "parameters": { "arity": 2 } },
      { "parameters": { "arity": 3 } },
      { "parameters": { "arity": 4 } },
      { "parameters": { "arity": 5 } },
      { "parameters": { "arity": 6 } },
      { "parameters": { "arity": 7 } },
      { "parameters": { "arity": 8 } }
    ],
    "evidence": [
      { "kind": "witness.generates" },
      { "kind": "witness.property_equals_parameter", "property": "projection_arity", "parameter": "arity" },
      { "kind": "witness.property_equals_parameter", "property": "column_list_arity", "parameter": "arity" },
      { "kind": "outcome.kind_in", "allowedKinds": ["accepted", "state"] }
    ]
  },
  {
    "id": "POSTGRESQL-SPEC-INSERT-CONFLICT-UPDATE",
    "level": "spec",
    "dialect": "postgresql",
    "statement": "WHERE INSERT ON CONFLICT DO UPDATE is retained, the generator SHALL produce witnesses inside the non-empty inference-spec subset.",
    "subject": { "kind": "family", "family": "postgresql.constraint.insert.conflict_update" },
    "evidence": [
      { "kind": "witness.generates" },
      { "kind": "outcome.kind_in", "allowedKinds": ["accepted", "state"] }
    ]
  },
  {
    "id": "POSTGRESQL-SPEC-GRANT-PARAMETER-TARGET",
    "level": "spec",
    "dialect": "postgresql",
    "statement": "WHERE GRANT and REVOKE parameter targets are retained, the generator SHALL produce witnesses inside the canonical configuration-parameter-name subset.",
    "subject": { "kind": "family", "family": "postgresql.constraint.grant.parameter_target" },
    "evidence": [
      { "kind": "witness.generates" },
      { "kind": "outcome.kind_in", "allowedKinds": ["accepted", "state"] }
    ]
  },
  {
    "id": "POSTGRESQL-SPEC-SELECT-SET-OPERATION",
    "level": "spec",
    "dialect": "postgresql",
    "statement": "WHERE SELECT set operations are retained, the generator SHALL produce accepted witnesses for arities 1 through 8.",
    "subject": { "kind": "family", "family": "postgresql.constraint.select.set_operation" },
    "cases": [
      { "parameters": { "arity": 1 } },
      { "parameters": { "arity": 2 } },
      { "parameters": { "arity": 3 } },
      { "parameters": { "arity": 4 } },
      { "parameters": { "arity": 5 } },
      { "parameters": { "arity": 6 } },
      { "parameters": { "arity": 7 } },
      { "parameters": { "arity": 8 } }
    ],
    "evidence": [
      { "kind": "witness.generates" },
      { "kind": "witness.property_equals_parameter", "property": "left_projection_arity", "parameter": "arity" },
      { "kind": "witness.property_equals_parameter", "property": "right_projection_arity", "parameter": "arity" },
      { "kind": "outcome.kind_in", "allowedKinds": ["accepted"] }
    ]
  },
  {
    "id": "POSTGRESQL-SPEC-SELECT-VALUES-CLAUSE",
    "level": "spec",
    "dialect": "postgresql",
    "statement": "WHERE VALUES clauses are retained in the SELECT family, the generator SHALL produce accepted witnesses for row arities 1 through 8.",
    "subject": { "kind": "family", "family": "postgresql.constraint.select.values_clause" },
    "cases": [
      { "parameters": { "arity": 1 } },
      { "parameters": { "arity": 2 } },
      { "parameters": { "arity": 3 } },
      { "parameters": { "arity": 4 } },
      { "parameters": { "arity": 5 } },
      { "parameters": { "arity": 6 } },
      { "parameters": { "arity": 7 } },
      { "parameters": { "arity": 8 } }
    ],
    "evidence": [
      { "kind": "witness.generates" },
      { "kind": "witness.property_equals_parameter", "property": "row_arity", "parameter": "arity" },
      { "kind": "outcome.kind_in", "allowedKinds": ["accepted"] }
    ]
  },
  {
    "id": "POSTGRESQL-SPEC-TEXT-SEARCH-TEMPLATE",
    "level": "spec",
    "dialect": "postgresql",
    "statement": "WHERE CREATE TEXT SEARCH TEMPLATE is retained, the generator SHALL produce engine-acceptable witnesses from that definition family.",
    "subject": { "kind": "family", "family": "postgresql.constraint.text_search_template.define" },
    "evidence": [
      { "kind": "witness.generates" },
      { "kind": "outcome.kind_in", "allowedKinds": ["accepted", "state"] }
    ]
  },
  {
    "id": "POSTGRESQL-SPEC-CREATE-OPERATOR-DEFINITION",
    "level": "spec",
    "dialect": "postgresql",
    "statement": "WHERE CREATE OPERATOR is retained, the generator SHALL produce witnesses from the required-procedure and safe-type subset.",
    "subject": { "kind": "family", "family": "postgresql.constraint.create_operator.definition" },
    "evidence": [
      { "kind": "witness.generates" },
      { "kind": "outcome.kind_in", "allowedKinds": ["accepted", "state"] }
    ]
  },
  {
    "id": "POSTGRESQL-SPEC-CREATE-AGGREGATE-DEFINITION",
    "level": "spec",
    "dialect": "postgresql",
    "statement": "WHERE CREATE AGGREGATE is retained, the generator SHALL produce witnesses from the required-SFUNC and STYPE subset.",
    "subject": { "kind": "family", "family": "postgresql.constraint.create_aggregate.definition" },
    "evidence": [
      { "kind": "witness.generates" },
      { "kind": "outcome.kind_in", "allowedKinds": ["accepted", "state"] }
    ]
  },
  {
    "id": "POSTGRESQL-SPEC-COMMENT-TYPE-REFERENCE",
    "level": "spec",
    "dialect": "postgresql",
    "statement": "WHERE COMMENT ON type-like objects is retained, the generator SHALL produce engine-acceptable witnesses from the object-name and safe-type subset.",
    "subject": { "kind": "family", "family": "postgresql.constraint.comment.type_reference" },
    "evidence": [
      { "kind": "witness.generates" },
      { "kind": "outcome.kind_in", "allowedKinds": ["accepted", "state"] }
    ]
  },
  {
    "id": "POSTGRESQL-SPEC-CREATE-CAST-TYPE-REFERENCE",
    "level": "spec",
    "dialect": "postgresql",
    "statement": "WHERE CREATE CAST is retained, the generator SHALL produce witnesses from the bounded canonical type subset.",
    "subject": { "kind": "family", "family": "postgresql.constraint.create_cast.type_reference" },
    "evidence": [
      { "kind": "witness.generates" },
      { "kind": "outcome.kind_in", "allowedKinds": ["accepted", "state"] }
    ]
  },
  {
    "id": "POSTGRESQL-SPEC-DROP-CAST-TYPE-REFERENCE",
    "level": "spec",
    "dialect": "postgresql",
    "statement": "WHERE DROP CAST is retained, the generator SHALL produce witnesses from the bounded canonical type subset.",
    "subject": { "kind": "family", "family": "postgresql.constraint.drop_cast.type_reference" },
    "evidence": [
      { "kind": "witness.generates" },
      { "kind": "outcome.kind_in", "allowedKinds": ["accepted", "state"] }
    ]
  },
  {
    "id": "POSTGRESQL-SPEC-CREATE-ASSERTION-CHECK-EXPRESSION",
    "level": "spec",
    "dialect": "postgresql",
    "statement": "WHERE CREATE ASSERTION is retained, the generator SHALL produce engine-acceptable witnesses from the safe boolean check-expression subset.",
    "subject": { "kind": "family", "family": "postgresql.constraint.create_assertion.check_expression" },
    "evidence": [
      { "kind": "witness.generates" },
      { "kind": "outcome.kind_in", "allowedKinds": ["accepted", "state", "environment"] }
    ]
  },
  {
    "id": "POSTGRESQL-SPEC-CREATE-ROUTINE-DEFINITION",
    "level": "spec",
    "dialect": "postgresql",
    "statement": "WHERE CREATE FUNCTION and PROCEDURE statements are retained, the generator SHALL produce engine-acceptable witnesses from the complete-definition subset.",
    "subject": { "kind": "family", "family": "postgresql.constraint.create_routine.complete_definition" },
    "evidence": [
      { "kind": "witness.generates" },
      { "kind": "outcome.kind_in", "allowedKinds": ["accepted"] }
    ]
  },
  {
    "id": "POSTGRESQL-SPEC-DROP-TYPE-OBJECT-NAME",
    "level": "spec",
    "dialect": "postgresql",
    "statement": "WHERE DROP TYPE and DROP DOMAIN statements are retained, the generator SHALL produce witnesses that stay inside the object-name subset.",
    "subject": { "kind": "family", "family": "postgresql.constraint.drop_type.object_name" },
    "evidence": [
      { "kind": "witness.generates" },
      { "kind": "outcome.kind_in", "allowedKinds": ["accepted", "state"] }
    ]
  }
]
