# Core concepts, independent platforms, and outward provider composition.
deptrac:
  paths: [./src]
  analyser:
    types: [class, use]
  layers:
    - name: Schema
      collectors:
        - type: classLike
          value: '^SqlFixture\\Schema\\.*'
    - name: TypeMapping
      collectors:
        - type: classLike
          value: '^SqlFixture\\TypeMapper\\.*'
    - name: Hydration
      collectors:
        - type: classLike
          value: '^SqlFixture\\Hydrator\\.*'
    - name: Plan
      collectors:
        - type: classLike
          value: '^SqlFixture\\Plan\\.*'
    - name: Generation
      collectors:
        - type: classLike
          value: '^SqlFixture\\Fixture\\.*'
    - name: MySql
      collectors:
        - type: classLike
          value: '^SqlFixture\\Platform\\MySql\\.*'
    - name: PostgreSql
      collectors:
        - type: classLike
          value: '^SqlFixture\\Platform\\PostgreSql\\.*'
    - name: Sqlite
      collectors:
        - type: classLike
          value: '^SqlFixture\\Platform\\Sqlite\\.*'
    - name: Provider
      collectors:
        - type: classLike
          value: '^SqlFixture\\Provider\\.*'
    - name: Faker
      collectors:
        - type: classLike
          value: '^Faker\\.*'
    - name: Syntax
      collectors:
        - type: directory
          value: 'src/Syntax/'
    - name: SqlParser
      collectors:
        - type: classLike
          value: '^SqlParser\\.*'
  ruleset:
    Schema: []
    TypeMapping: [Schema, Faker]
    Hydration: []
    Plan: []
    Generation: [Schema, Plan, TypeMapping, Hydration, Faker]
    Syntax: [SqlParser]
    MySql: [Schema, Syntax, TypeMapping, Faker, SqlParser]
    PostgreSql: [Schema, Syntax, TypeMapping, Faker, SqlParser]
    Sqlite: [Schema, Syntax, TypeMapping, Faker, SqlParser]
    Provider: [Schema, TypeMapping, Hydration, Plan, Generation, MySql, PostgreSql, Sqlite, Faker]
    Faker: []
    SqlParser: []
