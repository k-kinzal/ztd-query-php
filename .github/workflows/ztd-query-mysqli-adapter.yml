name: CI (ztd-query-mysqli-adapter)

on:
  push:
    branches: [main]
  pull_request:
    # Only this package's own sources gate the pull request run. A change to a
    # package this one depends on is validated by that package's own workflow,
    # and by the push-to-main run below, which rebuilds every package against
    # the merged tree. Listing dependencies here instead would make a stacked
    # series unmergeable: the first pull request in the stack would have to
    # carry every later package's changes to keep this workflow passing.
    paths:
      - 'packages/ztd-query-mysqli-adapter/**'
      - 'packages/phpstan-custom-rules/**'
      - '.github/workflows/ztd-query-mysqli-adapter.yml'

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref_name }}
  cancel-in-progress: true

defaults:
  run:
    working-directory: packages/ztd-query-mysqli-adapter

jobs:
  test:
    name: Test (PHP ${{ matrix.php }})
    runs-on: ubuntu-latest
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        # Each PHP resolves a different PHPUnit major, and their schemas reject
        # each other's strict settings, so every leg names the configuration
        # written for the major it will actually install.
        include:
          - php: '8.1'
            test-script: test:unit:10
          - php: '8.2'
            test-script: test:unit:11
          - php: '8.3'
            test-script: test:unit:12
          - php: '8.4'
            test-script: test:unit:12
          - php: '8.5'
            test-script: test:unit
    steps:
      - name: Check out the repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up PHP ${{ matrix.php }}
        uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240 # 2.37.2
        with:
          php-version: ${{ matrix.php }}
          extensions: mbstring, mysqli
          coverage: none

      - name: Install dependencies
        uses: ramsey/composer-install@65e4f84970763564f46a70b8a54b90d033b3bdda # 4.0.0
        with:
          working-directory: packages/ztd-query-mysqli-adapter
          dependency-versions: highest
          composer-options: --prefer-dist

      - name: Check installed platform requirements
        run: composer check-platform-reqs

      - name: Unit tests
        run: composer ${{ matrix.test-script }}

  parallel-test:
    name: Parallel Test Runner
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out the repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up PHP '8.5'
        uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240 # 2.37.2
        with:
          php-version: '8.5'
          extensions: mbstring, mysqli, pcntl
          coverage: none

      - name: Install dependencies
        uses: ramsey/composer-install@65e4f84970763564f46a70b8a54b90d033b3bdda # 4.0.0
        with:
          working-directory: packages/ztd-query-mysqli-adapter
          dependency-versions: highest
          composer-options: --prefer-dist

      # What the version matrix cannot answer: whether the suite survives being
      # split across processes. That does not depend on the PHP version, so it
      # is asked once, on the highest one supported.
      - name: Unit tests, split across processes
        run: composer test:parallel

  test-integration:
    name: Integration (PHP ${{ matrix.php }})
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        # As in the unit job: each PHP installs a different PHPUnit major, and a
        # configuration written for a later one does not validate on an earlier.
        include:
          - php: '8.1'
            test-script: test:integration:10
          - php: '8.2'
            test-script: test:integration:11
          - php: '8.3'
            test-script: test:integration:12
          - php: '8.4'
            test-script: test:integration:12
          - php: '8.5'
            test-script: test:integration
    steps:
      - name: Check out the repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up PHP ${{ matrix.php }}
        uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240 # 2.37.2
        with:
          php-version: ${{ matrix.php }}
          extensions: mbstring, mysqli
          coverage: none

      - name: Install dependencies
        uses: ramsey/composer-install@65e4f84970763564f46a70b8a54b90d033b3bdda # 4.0.0
        with:
          working-directory: packages/ztd-query-mysqli-adapter
          dependency-versions: highest
          composer-options: --prefer-dist

      - name: Integration tests
        run: composer ${{ matrix.test-script }}

  lint:
    name: Lint (PHP ${{ matrix.php }})
    runs-on: ubuntu-latest
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        php: ['8.1', '8.2', '8.3', '8.4', '8.5']
    steps:
      - name: Check out the repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up PHP ${{ matrix.php }}
        uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240 # 2.37.2
        with:
          php-version: ${{ matrix.php }}
          extensions: mbstring, mysqli
          coverage: none

      - name: Install dependencies
        uses: ramsey/composer-install@65e4f84970763564f46a70b8a54b90d033b3bdda # 4.0.0
        with:
          working-directory: packages/ztd-query-mysqli-adapter
          dependency-versions: highest
          composer-options: --prefer-dist

      # --strict is left off: this package reaches its siblings through path
      # repositories, which Composer can only be given as "@dev", and --strict
      # fails any unbound constraint. Everything else about the file is checked.
      - name: Validate composer.json
        run: composer validate --no-check-publish

      - name: Coding standards
        run: composer format:check

  static-analysis:
    name: Static Analysis
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out the repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up PHP '8.5'
        uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240 # 2.37.2
        with:
          php-version: '8.5'
          extensions: mbstring, mysqli
          coverage: none

      - name: Install dependencies
        uses: ramsey/composer-install@65e4f84970763564f46a70b8a54b90d033b3bdda # 4.0.0
        with:
          working-directory: packages/ztd-query-mysqli-adapter
          dependency-versions: highest
          composer-options: --prefer-dist

      - name: Static analysis
        run: composer phpstan

  mutation:
    name: Mutation Testing
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - name: Check out the repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          # Scoring a pull request means diffing it against its base branch,
          # which needs more than the single commit a shallow clone provides.
          fetch-depth: 0

      - name: Set up PHP '8.5'
        uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240 # 2.37.2
        with:
          php-version: '8.5'
          extensions: mbstring, mysqli, pcntl
          # Infection needs a coverage driver. pcov is several times faster
          # than Xdebug and Infection collects nothing Xdebug alone can provide.
          coverage: pcov

      - name: Install dependencies
        uses: ramsey/composer-install@65e4f84970763564f46a70b8a54b90d033b3bdda # 4.0.0
        with:
          working-directory: packages/ztd-query-mysqli-adapter
          dependency-versions: highest
          composer-options: --prefer-dist

      - name: Check installed platform requirements
        run: composer check-platform-reqs

      # Infection's own initial test run cannot be used against a strict toolkit
      # PHPUnit configuration: it stops at the first byte on STDERR and adds
      # stopOnDefect, so one risky test would leave the gate scoring a near-empty
      # coverage report as if it had scored everything.
      - name: Generate coverage for mutation testing
        run: >-
          php -d memory_limit=1G vendor/bin/phpunit
          --testsuite=unit
          --no-extensions
          --do-not-fail-on-risky
          --coverage-xml build/infection-coverage/coverage-xml
          --log-junit build/infection-coverage/junit.xml

      # Size memory_limit for the reports rather than for the run: Infection holds
      # every mutant result and encodes all of them into build/infection.json once
      # the score is printed, so a large source tree exhausts a limit the scoring
      # itself never came close to.
      - name: Mutation testing on changed lines
        if: github.event_name == 'pull_request'
        run: >-
          php -d memory_limit=4G vendor/bin/infection
          --configuration=infection.json5
          --threads=max
          --coverage=build/infection-coverage
          --skip-initial-tests
          --git-diff-lines
          --git-diff-base="origin/${GITHUB_BASE_REF}"
          --ignore-msi-with-no-mutations
          --min-msi=85
          --min-covered-msi=85
          --no-progress
          --logger-github

      - name: Mutation testing on the whole source tree
        if: github.event_name != 'pull_request'
        run: >-
          php -d memory_limit=4G vendor/bin/infection
          --configuration=infection.json5
          --threads=max
          --coverage=build/infection-coverage
          --skip-initial-tests
          --no-progress
          --logger-github
